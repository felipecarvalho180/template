import { tv } from "tailwind-variants";

export const card = tv({
  slots: {
    form: "bg-white rounded-xl p-8 flex flex-col gap-6 mt-10 sm:mx-auto sm:w-full sm:max-w-sm lg:w-full lg:max-w-4xl",
  },
});
