/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    internalUrl: 'https://template-beta-ten.vercel.app',
    baseUrl: 'https://jsonplaceholder.typicode.com'
  }
}

module.exports = nextConfig
